REST разделён по сущностям
DTO изолируют Entity
JWT минимальный, stateless
PostgreSQL через Docker
Связи @ManyToOne, @OneToMany — правильная модель данных
Запуск:
docker-compose up -d
./mvnw spring-boot:run
